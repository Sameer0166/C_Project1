#include<stdio.h>
#include<math.h>
int main(){
    float i,n;
    printf("enter the number to find the power of a number");
    scanf("%f",&i);
    printf("sqaure is = %f",  n=pow(i,2));
    return 0;
}